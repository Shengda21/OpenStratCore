/target
**/*.rs.bk
node_modules/
dist/
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
.codex/
.env
.env.*
!/.env.example
# assets/generated/ is product art (unit/terrain sprites + manifest) — tracked & shipped with the web build.
# Training output dirs (anchored, so web/public/runs/ — the shipped demo replay — stays tracked).
/runs/
python/runs/
*.replay.json.snapshots
.DS_Store
