# 规则覆盖清单（coverage）

> 这是**自主模式的进度真源**。每次会话开始：读本表 → 找第一个未勾选项 → 去 `TASKS.md` 找对应任务 → 实现 → 过绿门 → 回来勾选并 `git commit`。
> 状态：`[ ]` 未做 · `[~]` 进行中 · `[x]` 完成（done = 对应 TASKS 验收命令通过 + 有确定性测试 + 重放不破）。

## M0 · 地基（scaffold 已给，主要是补实现）
- [x] 六角格坐标/距离/邻居/连线（`hex.rs` 已实现+测试）
- [x] 确定性 RNG（`rng.rs` 已实现+测试）
- [x] 类型/地图/想定/状态 serde（`types.rs`）
- [x] 规则加载 rules-as-data（`rules.rs`，需接 `config/tables/*`）〔T0.1〕
- [x] 事件队列引擎骨架（`engine.rs`：step/advance/observe/snapshot）〔整数 Tick 化于 T0.2〕
- [x] 录制→重放→逐 tick 一致回归（`/replay-verify` 真正落地）〔T0.2〕
- [x] `make verify-all` 全绿〔T0.3〕

## M1 · 纵向切片 MVP（坦克+步兵，可玩最小闭环）
- [x] 机动状态机：正常/一级冲锋/二级冲锋/掩蔽/半速 互斥 + 75s 转换（三.1）〔T1.1；速度/疲劳归 T1.2/T1.3〕
- [x] 车辆机动：地形系数 + 坡度系数 + 不可通行 + 路障（三.1.2 / `terrain_movement.json`）〔T1.2；大河扩 Terrain。**路障已接 W14**:三.1.2g「路障地形不可通过/地面算子(人员/车辆)无法通过设有路障的区域」——step_ticks 在目标格有 kind=="roadblock" 设施时对地面算子返回 None(同不可通行地形),plan_hop 即在路障前一格 settle 停;空中算子(is_air 早返)飞越不挡。路障系静态板面特征(非工事不入 state.facilities、不中途生成)→直接读 self.scenario.facilities,无需 MoveArrive 复查。测试 roadblock_blocks_ground_not_air:坦克/步兵止于路障前格、攻击直升机飞越达对侧〕
- [x] 人员机动：地形无关 + 高差>60m 半速 + 冲锋倍率/疲劳（三.1.4）〔T1.3 机制层(infantry_step_time_seconds 含半速/冲锋倍率、infantry_fatigue_per_hex、infantry_mode_allowed)。**W10 半速 mode-on-move 已接**:move_to mode:"half" 即本次半速(人员/车辆均 ×2 时,per-move 不走 75s 转换)、move_to 保留 Half/March 态、settle 回 Stopped(半速非持久姿态),解锁雷场通路前置。采纳 Codex 2 条:行军态忽略 mode:"half"(不绕过三.1.3e/f)、set_mode "half" 与初始 state:"half" 一律拒(半速仅 per-move,免持久 Half 不能开火)、schema set_mode enum 去掉 half。已知边(留后续):多跳半速移动中途被压制,车辆后续跳丢失 Half(与「压制中止移动」更大模型纠缠,另议)。**一二级冲锋 mode-on-move + 活体疲劳已接**〔W12;**三.1.4 全套完成**:RuntimeUnit 加 fatigue: i64 字段(serde 默认 0 跳过);move_to mode:"charge1"/"charge2" 即本次一/二级冲锋(步兵专属,×2/×4 速,per-move 不走 75s,与 half 同范式),move_to 入口按 infantry_mode_allowed 门(非步兵冲锋拒、一级疲劳拒二级冲锋、二级疲劳拒一切机动含常速移动);MoveArrive 入格时按当前冲锋态 infantry_fatigue_per_hex 累积疲劳(+1/冲锋格);plan_hop 起跳前复查 infantry_mode_allowed(疲劳累到一/二级即原地 settle 停止冲锋,玩家须休整后重发);settle_stopped 入空闲即 arm 降疲劳——FatigueDecay 事件每 infantry_fatigue_decay_seconds(75s)−1、fatigue_gen 防陈旧(move_to 起跳 bump 使移动中止恢复计时、降到 0 即不再排期),恢复链至 0;set_mode "charge1"/"charge2" 一律拒(冲锋仅 per-move,免持久冲锋态错误禁火);observe() 本方步兵补 fatigue 字段(仅己方>0,敌触点不带,规则#5);schema move_to mode enum 改为 normal/half/charge1/charge2(去 march,行军非 per-move)、set_mode enum 去 charge(仅 normal/march/cover)、ownUnit 加 fatigue。测试 engine::charge_and_fatigue:一级冲锋 2 格半时到位即二级疲劳停、二级冲锋 1 格即一级疲劳停、疲劳 2→1→0 每 75s 降、二级疲劳禁动/一级疲劳禁二级冲锋/set_mode 冲锋拒/坦克冲锋拒。**解释性默认**:冲锋为 per-move 爆发(非持久姿态,免「站立冲锋」语义);疲劳恢复以连续静止计——move_to(实际移动)重置 75s 恢复计时,set_mode/开火/掩蔽不重置(疲劳系机动消耗)〕
- [x] 通视：连线插值 + 居民/丛林 +1 + 高看低 +1（三.6）〔T1.4〕
- [x] 观察：完整 8 行距离表 + 掩蔽/地形减半（三.7 / `observation_distance.json`）→ 战争迷雾观测（硬规则#5）〔T1.5；含 hex line_to 整数化确定性修复〕
- [x] 直瞄对车辆流水线 A（附1.2/1.4/1.5/1.6）〔T1.7；引擎 fire_direct 接线归 T1.8〕
- [x] 直瞄对人员流水线 B（附1.1/1.7/1.8）〔T1.6；引擎 fire_direct 接线归 T1.8〕
- [x] 直瞄通用：锁定/展开/冷却75s/射击条件/行进间(仅坦克主炮)（三.8）〔T1.8；引擎 fire_direct 接线；迷雾门前置防探测+Tank 行进间守卫+水上坦克修正接线〕
- [x] 夺控（中心+周边6格无敌；空中/炮兵不可）（三.5）〔T1.9；engine `capture` op；真态裁决+泛化拒绝防探测〕
- [x] 堆叠 ≤4（三.4）〔T1.9；`mechanics::stacking_allows_entry` 纯谓词，移动接线随 T1.10 寻路层落地〕
- [x] 一局自博弈冒烟不崩 + 切片场景 `scenarios/demo_skirmish` 跑通〔T1.10；move_to 逐格寻路实装（地形/坡度/高差速度 + 堆叠门 + 生成号抢占）；sim_smoke 脚本对局闭环：机动→接敌开火→夺控 CP1→确定性重放，纳入 verify-all〕

## M2 · 火力与侦察扩展
- [x] 掩蔽规则细节：坦克射击中断、引导不退出/被引导退出（三.3）〔T2.1；掩蔽转换生成号可中断（坦克射击/压制打断）+ 射击解除已掩蔽 + 压制中不可转掩蔽 + 三.3g 已掩蔽被压制不解除 + 被压制不可射击；引导(d)留谓词待 T2.6〕
- [x] 上下车 75s + 压制中断（三.2）〔T2.2；RuntimeUnit.carried_by + mount/dismount op（生成号可中断转换）+ 压制中断(三.2d/e) + 被载随车移动/不可独立受令/迷雾隐藏/不计堆叠;承运者死亡处置(规则缺省的解释性默认)〕
- [x] 间瞄流水线 C（三.9 / 附2 全表）〔T2.3；combat 解析器(校射→散布→命中/偏离→1d6修正)+ 引擎 plan/cancel_indirect、150s飞行→裁决→300s爆炸(进入受裁)、对己方误伤、炮兵机动取消、300s冷却、目标校射仅敌方〕
- [x] 巡飞弹（三.10 / 8s格/+200m/侦察2/1200s/75s发射）〔T2.4；新 `units` 模块；launch_loitering(75s发射、每车一枚、双发射竞态防护)、1200s自毁、承运车毁则同毁(载/已发射)；侦察走观察表。**打击/飞行已接 W16**〔三.10f 打击 + 飞行:**关键发现**——巡飞弹**确无专属结果表**(交叉核验确认,三.10f 仅述「发现即可打击」时机),但 `attack_level_vs_vehicle.json`(附1.2)**已含 巡飞弹 行**(range 2/levels[5,5,5],已验证转写)→打击复用**直瞄对车流水线 A**(共享 vs-vehicle 结果表+装甲修正),**全表数据驱动无臆造值**(人选定 A 案)。combat::loitering_strike(无武器态/停止门——巡飞弹空中即打三.10f,fire_mods+resolve_direct_vs_vehicle,高差按地形 elev,+200m 空袭高差延后);engine strike_loitering op(须已部署巡飞弹、loadout 取「巡飞弹」rules-as-data、目标缺/己/死/离场→泛化错、can_observe 雾门崩泛化防探敌#5 同 aa_fire 范式、命中后 apply_direct_fire+一次性自毁 mirror LoiteringSelfDestruct〔排期自毁后续 alive=false no-op〕);飞行=deployed 巡飞弹 move_to(空中 8s/格,既有移动机制)。config 补 loadout.loitering_munition=["巡飞弹"](双 config)、schema strike_loitering op。**解释性默认**:打击一律走 vs-vehicle(人选 A,含对人员按其装甲〔无甲〕过 A);一次性 kamikaze(打击即耗弹自毁,三.10f 未明取自然解);+200m 空袭高差利延后。测试 loitering_strike_and_flight(发射中禁动/部署后飞行/出侦察距打击崩泛化/在距打击耗弹/已耗不可再打)〕
- [x] 无人机（三.11）〔T2.5；空中单位 +200m 越障 LOS(`line_of_sight_alt`/`air_altitude_levels`,rules.air 配置)+ 空中机动按基速无视地形/坡度;侦察2格/被见相邻走观察表〕
- [x] 引导射击（三.14；步兵/无人车/无人机引导重型导弹）〔T2.6；`combat::guided_fire`(校验三.14a/c/d/e + 经流水线A/B裁决)+ 引擎 guide_fire op(无人机引导任意重导车 + 75s准备 + 迷雾门);步兵/无人车 隶属引导待 三.13 UGV 模型〕
- [x] 侦察型战车 50格 + 炮兵校射雷达开机75s（三.19）〔T2.7；侦察战车50格走观察表;radar_on op(静止/未压制开机、75s、压制打断开机、生成号)、开机→间瞄校射降为≥格内校射(floor)、机动即关、压制中失效压制结束自动恢复〕
- [x] 同格交战（三.15；距离0/25s/优先级/惩罚打击）〔T2.8；单位→武器 loadout(rules.loadout)+ combat::same_hex(优先级坦克>战车>其他>人员/少班组、自动选武器、距0裁决)+ 引擎触发(移动/开局)、先入序、25s轮、即时射、脱离惩罚、三.15c格外不可射入、三.15e打断上下车/掩蔽/雷达、武器冷却、生成号、僵局终止守卫(硬规则#1);并修步兵轻武器对车辆 ref 表(连带修流水线A)〕

## M3 · 空中与特种
- [x] 武装直升机（三.12 / 4s格/对人10对车25）〔T3.1；观察(对人10/对车25/被见25/无人机相邻)走表 + 空中机动4s/格无视地形 + +200m越障 LOS,均由 T2.5 基建支撑;本任务以 units::attack_heli 验证锁定〕
- [x] 运输直升机（三.16 / 高低超低空 +500/200/20m / 装卸75s）〔T3.2；高度状态机(switch_altitude 75s 仅相邻)+ 超低空半速 + 索降装卸(复用 carriage:超低空/开阔地/空载/容量≤4)+ 母机毁则同毁;**三高度通视已接线**〔W3;heli_alt 移到 RuntimeUnit(单一真源,替掉引擎侧 map)→ air_altitude_levels 按算子当前高度带读 heli_altitude_meters[high/low/very_low],超低空(+20m)正确看得少/被看得少(三.16b,迷雾门 can_observe 全路径生效);Engine::new 校验 heli_altitude_meters 三键齐全(纵深防御,schema 亦 required);observe() 本方侧补 altitude 字段(仅己方、敌触点不带,schema ownUnit 加 enum)。测试:山脊遮挡下低/高空可见、超低空隐蔽 + 本方观测读出高度带〕。**整体战损已接**〔W13;三.16h 运输直升机被打「作为整体接受毁伤裁决」——apply_direct_fire(直瞄/间瞄/雷场共用毁伤汇聚点)在目标为 TransportHeli 且存活时,把同一 Destroyed(n)/Suppress 结果转发给每个 carried_by==该机的存活装载班(同等损失,递归深度1因班不再载机);**仅限直升机**:地面车辆承运者反而按三.2 遮蔽搭载免部分战损(只整毁才同毁,既有 handle_carrier_death);直升机整毁时装载随承运者死亡逻辑同毁(不重复转发,故只在存活命中时转发)。采纳 Codex [High]:转发仅对 can_be_passenger(步兵/防空导弹班)装载,既合三.16h「装载单位=索降兵」语义又把递归限死深度1(班绝非承运者,转发不再入本支,杜绝直升机载直升机环致无限递归挂死);并补 Engine::new 轻量 carried_by 校验(承运者须存在·同侧·非自身;因 carried_by 双关三.2 班搭载与三.10 巡飞弹载于发射车,不在此约束类型)。测试 transport_heli_integral_loss(直接驱动 apply_direct_fire 定结果免RNG):部分战损 H-1 则装载班同-1且存活被压制、地面步战车承运 C 免伤不压制(三.2 遮蔽)、纯压制共享给装载、整毁带走装载、自载拒、直升机载直升机不递归〕〕
- [x] 无人战车（三.13；依托有人战车，可引导）〔T3.3；RuntimeUnit/ScenarioUnit.affiliated_to(隶属链,载入校验:必属同方有人战车非自身/敌方/缺失,UGV必须有隶属 三.13b)+ guide_fire 放开非无人机:步兵/无人车仅引导所隶属车辆(三.14b)、UGV须下车+停止(三.13c)、武器/通视/射程经 combat::guided_fire;母车毁则 UGV 同歼(三.13d,经 handle_carrier_death 共享死亡汇聚点;仅 UGV,隶属步兵不连带)+ 机动/侦察/直瞄同战车(a);迷雾不泄漏隶属链(规则#5,3字段白名单+锁测)〕
- [x] 防空流水线 D（三.18 / 附3 全表；多次裁决=车班数）〔T3.4；combat::air_defense 纯裁决核:攻击等级(附3.1,含最小射程/平台尾值/超射程 None)→歼灭阈值钻石(附3.2)→多次裁决=车/班数(三.18f,任一歼灭即毁,0车班/超射程不掷骰)+ 0级/最小射程不可交战;附3.1 已逐格交叉核验(便携/车载中段修正);射速上限(15s×10/75s×4/15s×4)数据在 timings.fire_intervals。**引擎 aa_fire op 已接线**〔W1;engine `aa_fire` op:三.18e 观察门(can_observe 用 rules.observation 的导弹小队/车 50格·无人机 25格·巡飞弹不可,迷雾门归引擎 #5)+对空目标限定+流水线 D 裁决(歼灭→经 apply_direct_fire 致死)+射速(每 air.aa[type].interval 仅冷武器态,不锁机动)+弹药上限(air.aa[type].max_shots,三.18g,aa_shots 计数);air.aa 块为引擎可消费的 rules-as-data(weapon/interval/max_shots,严格解析+schema-required),timings.fire_intervals 为附录转写式〕
- [x] 聚合解聚（三.17 / 75s / 总班≤4 / 弹药平均向下取整）〔T3.5；mechanics::{supports_aggregation,aggregate,split_squads,split} 纯算:聚合班数累加(≤cap)+弹药⌊平均⌋(三.17g,u64防溢)、解聚 4=2+2/3=2+1/2=1+1+两子保留原弹药(三.17g)、炮兵不支持(三.17i)、超cap拒绝;cap 走 control.max_squads_per_unit(rules-as-data)+timing.aggregate_split:75。**引擎 op 已接 W15**〔三.17 聚合解聚全套(本仓**首个运行中增删算子**):aggregate op(75s 把同类型/同格/同侧 own 班 merge 入发起者,合计班数≤cap;被并算子 alive=false 离场如死亡——陈旧 gen 表项 lazy no-op,免逐表清理)、split op(75s 裂为发起者保留较大半+生成子算子 id=`{父}#{spawn_ctr}`全局单调确定性、`#`在 Engine::new 保留禁现于想定 id 故子 id 永不撞);AggSplitInTransit/aggsplit_op/aggsplit_gen 镜像工事转换范式、Event::AggSplitComplete 完成时二阶复查(双方存活/Stopped/同格/同类型/未压制)、interrupt_aggsplit 经 interrupt_mounts_on_suppress 兼覆压制(三.17d)与同格开战中止;aggsplit_check 门(地面非炮兵 supports_aggregation、Stopped、未压制、未载/被载/驻守/行军、非同格、对发起+被并双查载员三.17f/巡飞弹三.17e)。**采纳两轮对抗复审(Codex+4-lens Workflow)所有真问题**:① 同 tick 改派守卫(busy_until==clock 否则旧完成不动手,镜像 MineLayReady);② 聚合禁吞「无人战车隶属母车」防三.13d 孤儿(否则 UGV 指向死母);③ spawn_ctr 插入前 contains_key 防 u64 回绕静默覆盖(撞则优雅放弃解聚)。复审误报已验证既有防护:reject_if_busy(被并方)防双吞/级联、aggsplit_check 双查覆盖被并方载弹、三.13d+存活复查覆盖死母。**解释性默认/延后**:弹药平均三.17g **延后**(RuntimeUnit 无弹药字段,聚合解聚仅作用班数,mechanics 弹药入参传0);乘员/巡飞弹**门控**(载/被载/发射车不可聚解,免乘员再分配歧义,三.17f 保守解);战前部署三.17h **延后**(编辑器域非运行时)。测试 engine::aggregate_and_split(聚合/裂生子/ctr 递增/重放确定性同令同roster/压制中止/门/同 tick 守卫/双吞拒)+aggregate_ugv_master_gate;replay 不变(demo 逐 tick 一致)〕
- [x] 行军（三.1.3 / 乡村40 一般60 等级90 / 沿路x2 / 75s）〔T3.5… T3.6；mechanics::{march_step_time_seconds,road_allows_direction,march_allows_entry} 纯算:道路等级 km/h→秒/格(hex_size_meters 200,无视地形/高差 三.1.3b;rail 无行军)、沿路走向(connects)、三.1.3d 阻挡(下一格有停止/非行军单位则阻挡,行军纵队不自阻,被载不计,权威谓词同 stacking,迷雾外显归引擎)。**完整行军引擎已接线**〔W4;step_ticks 接 march_step_time_seconds(March 态车辆按所在路等级 km/h,无视地形)、plan_hop 接沿路路由(本格出向+目标格反向连通,离路即 settle)+三.1.3d 阻挡(plan 时与 MoveArrive 到达时双查,镜像 stacking)、set_mode("march") 入口门(车辆非炮兵+可行军路等级,立即置 March 态以在 150s=武器锁定75+转换75 窗口内禁火/禁发巡飞弹,busy 期间禁动)、move_to/settle_stopped 保持 March 态(行军中/停在路上仍 March,退出走 set_mode 75s 三.1.3f)、三.1.3e 禁令(直瞄/引导经 combat Stopped 门在观察门之后拒、aa_fire 显式 reject_if_marching 置于观察门后保迷雾、launch_loitering 拒行军承运车)、Engine::new 拒非车辆初始 March 态。敌方行军单位不阻挡而由三.15同格交战接管;充电/半速 mode-on-move 仍未接(非行军范畴)〕

## M4 · 工事 / 雷场 / 支援
- [x] 车辆工事 / 人员战斗工事 / 人员隐蔽工事（三.20 / 容量5 / 12格 / 进出75s）〔T4.1；mechanics::FortKind(车辆/战斗/隐蔽)+谓词:准入(人员→人员工事/车辆→车辆工事/空中不可)、暴露观察射击距离(车辆/战斗12格、隐蔽仅本格0)、可否射击进出(隐蔽不可)、战果继承(战斗工事人员继承、车辆工事不继承)、地形修正(隐蔽不受地形)、超容退出(班数>剩余容量);容量5/暴露12格/进出75s 走 rules-as-data(fortification 块+timing.fortification_transition,schema-required)。**引擎工事 op 部分接线**〔W5;人员隐蔽工事(三.20.3)整段已接:RuntimeFacility 入 State(想定 facilities 载入,FAC{idx} id)+ RuntimeUnit.inside_facility(镜像 carried_by);新 is_on_board()=carried_by&&inside_facility 替换 ~17 处"在盘"过滤(观察/选标/间瞄/同格/堆叠/夺控/天基,迷雾全覆盖);enter_facility/exit_facility op(75s、三.20c 准入、三.20d 容量〔班〕、本格停止、完成时容量复查防竞态);驻守全程隐蔽(submit 顶层门令驻守算子除 exit 外一律 inert,按本方侧拦免泄敌);三.20.3d 敌入本格→暴露+自动离开+同格(守方先裁,入工事即盖戳 entry order);observe 本方补 insideFacility + 本方/无主工事 facilities(敌工事不显)。schema 加 enter/exit_facility op、ownUnit.insideFacility、observation.facilities。采纳 Codex 6 条(4High/2Med):observe 观察者也按 is_on_board 过滤(驻守不向外侦察)、occupantSquads 只数本方(中立工事不泄敌班数)、enter 对不可用 id(敌方/不存在)统一报错(防 FAC{idx} 探敌)、入工事完成时复查(被压制/敌入本格则中止不得遁入隐蔽)、exit 完成复查堆叠、三.20.3d 自动离开清 busy。W6 续:人员战斗工事(三.20.2)整段已接:RuntimeFacility 加 armor(三.20.1d/2d)、Armor::parse;敌战斗工事在 12格+通视内入 observe.enemyFortifications(仅 id/kind/at、不泄占用);fire_direct 加 targetFacility 支(combat::direct_fire_at_works:工事即车级目标、按工事自身装甲+居民地/工事地形过 A 流水线、观察门=12格+LOS 而非侦察表),战果经 apply_fire_to_personnel_works 直接继承占员(Destroyed 按 id 序分摊班损、Suppress 全压制,三.20.2e);车辆/战斗工事驻守可向外 直瞄/防空(submit 门 + observe 观察者门按 fortification_can_engage 放行),全程隐蔽不破;schema 加 fire_direct.targetFacility + observation.enemyFortifications。**解释性默认(见 interpretive-decisions)**:多占员战斗工事杀伤按 id 序分摊;间瞄出工事不允(仅直瞄);工事被打地形取 UrbanOrWorks(不叠底格地形);工事被打高差修正暂记0。采纳 Codex 4 条:facility_observed_by 观察者也含 can-fire-out 驻守(surfacing 与单位侦察一致)、schema fire_direct oneOf 强制恰一目标、enemyFortifications items required、apply_fire_to_personnel_works 传播错误不吞。W7 续:车辆工事(三.20.1)接线,**三类工事引擎 op 全部完成**:enter 放行车辆(fortification_admits 车→车工事)、fire_direct_at_facility 推广到 can_engage(战斗+车辆)并按 kind 分派战果——车辆工事走 apply_fire_to_vehicle_works(车辆不继承 三.20.1e、工事容量吸收 Destroyed(n)、超容〔占员班>剩余容量〕则瞬时退出+暴露+同格 三.20.1f/20e);敌车辆工事亦在 12格 surfacing。采纳 Codex 2 条:工事进出转换加 per-unit generation(facility_gen)——强制超容退出/三.20.3d 暴露均 bump，过期 Facility{Enter,Exit}Ready 自动 no-op(防陈旧事件清错 busy/撤销重入);超容退出保留入工事时盖的 same_hex_order(不覆盖，免守方落到后入敌之后)。W8 续:间瞄打工事接线(apply_indirect_at 落点格有 can_engage 工事则按工事装甲+居民地地形过 apply_indirect_to_target、战果同样分派 personnel/vehicle 继承;隐蔽工事免疫 三.20.3c;find_map 取首个 can_engage 工事免被隐蔽工事遮挡;Codex Low 采纳)。**工事(三.20)引擎 op 全部完成:三类×直瞄/间瞄/进出/驻守开火/暴露/超容全通**〕
- [x] 雷场流水线 E（三.21 / 附4；布雷车50格×3、扫雷/坦克半速开辟）〔T4.2；combat::minefield_damage(_for_roll)纯裁决:base(附4.1 车辆/人员按 UnitType)+装甲修正(附4.2 null=0,绕7)→floor≥0(班数);minefield_category(步兵/导弹小队=人员,余地面=车辆);mechanics::{can_lay_mines=布雷车,can_clear_minefield=扫雷车/坦克};布雷50格×3/布雷75s/观察10格 走 rules-as-data(minefield 块+timing.minefield_lay,schema-required)。**引擎雷场 op 部分接线**〔W9;布雷+入场裁决已接:RuntimeMinefield 入 State(想定 facilities kind"minefield"载入,按格 Axial 键)、lay_mines op(火箭布雷车、静止未压制、50格内、minefield_lay 75s、minelayer_lays≤3、minelayer_gen 防陈旧、完成时 压制/死 中止不计次)、MoveArrive 入未开辟雷场格地面单位过 combat::minefield_damage(2d6→附4)经 apply_direct_fire(三.21e 双方均有效含布雷方、空中 is_ground 免、雷场 persist 不消耗)、observe 补 minefields(本方或本方地面单位 10格+通视内、仅格不显主,规则#5);schema 加 lay_mines op + observation.minefields。**解释性默认**:入场伤害归进入算子;触雷后雷场存留(附4 是毁伤非消除)。采纳 Codex 4 条:MineLayReady 加「完成 tick 被改派则中止不清 busy」守卫(busy_until==clock 才认本次布雷)、压制路径(interrupt_*_on_suppress)经 minelay_in_flight 集合中止在飞布雷(免依赖完成时快照)、入场触雷致死后 MoveArrive 立即 alive 复查 return(死者不触发三.20.3d暴露/同格/续跳)、重复布雷格 submit 拒+MineLayReady or_insert 不覆盖。W11 续:通路/扫雷开辟(三.21f-i)接线,**雷场(三.21)全套完成**:RuntimeMinefield.cleared→cleared_by(BTreeSet<SideId> 单侧可见 三.21h);MoveArrive 入雷场格三分支——扫雷车/坦克半速(state==Half)且本侧未开辟→开辟通路(入 cleared_by)且不受伤(三.21f)、本侧已开辟→沿通路免裁决(人员任速/车辆半速 三.21i)、否则附4 裁决(三.21e);config 补 minelayer/minesweeper 基速 20s/格(原缺,W9 布雷车只静止布雷未动)。**解释性默认**:通路 per-hex per-side(同道路方向绑定 三.21g 暂略)、扫雷者开辟时免伤、开辟需半速。minelayer/minesweeper 基速 20 待复核(可辩护默认)〕
- [x] 天基侦察算子（三.22；只显编号最小、不可直瞄）〔T4.3；新增 UnitType::SpaceRecon + mechanics::{is_space_recon,can_be_targeted=!天基(三.22b 不可被攻击),space_recon_reveals(本方有天基→敌每格只显编号最小、排除被载/工事内<工事待接>),can_capture 排除天基};机动惰性结构性(无基速→step 报错);schema 加 space_recon。**引擎已接线**〔W2;can_be_targeted 门接入全部开火路径(直瞄/引导/防空显式目标 + 间瞄受害者 + 同格触发/攻击者/选标/惩罚,经 Codex 补齐位置型路径);space_recon_reveals 接入 observe():本方有天基→敌每格编号最小算子以 track:"space" 缩略航迹(仅 id/type/at、无车班弹损,三.22c)入 enemyUnits,非直接观察故不可直瞄(三.22d,迷雾门 can_observe 自然拦);堆叠/夺控区计数排除天基(三.22b 不入盘)。工事内排除留 工事 engine op〕
- [x] 设施：间瞄炮火区进出裁决（一.1.3）〔T4.4；mechanics::barrage_zone_triggers(仅地面算子,空中飞越)+combat::barrage_zone_adjudicate(地面算子进出炮火区→以命中复用 T2.3 间瞄流水线:附2 结果行+战果修正→Outcome);确定性。**炮火区作为设施在盘面建模+进出检测留引擎接线**;命中(非重掷散布)为解释性默认(区即落点区)〕

## M5 · 接口与产物
- [x] LLM-agent 工具接口对齐 `schemas/llm_tools.schema.json`（move/fire/plan_indirect/…）+ 迷雾观测〔T5.1；engine.observe() 自带 tick(floor 决策步)+ 修正 objectives 用实时控制(夺控点 owner 公开=胜负记分板,非迷雾敌情;中立/未设省略)+ ownUnit.mountedIn 仅乘车时含(避免 null 违反 schema);llm_agent.py 加 --dry-run(逐 tick 用 jsonschema 校验每个迷雾观测+动作表对齐 llm_tools.schema,违规即非零退出)+ tick_s 走 rules.decision_tick_seconds + load_inputs UTF-8 修复。done-when: validate prompts/examples 通过 + dry-run 20 观测/20 动作表全 schema-valid〕
- [x] RL：PettingZoo 真 Rust 后端（替换 mock），自博弈可收敛冒烟〔T5.2；_RustBackend 由占位改真环境:Discrete(4)动作头(停/移-q/移+q/开火,作用于本方编号最小 lead 单位)+自动夺控、7 维 obs **纯由 observe() 迷雾投影**(规则#5,id 排序确定性)、奖励整形读 snapshot god-view(敌损+/己损-/趋目标+/时惩-/夺控或歼灭 ±10)、夺控或一方全歼即终止;tick_s/队伍归一/读 rules-as-data(decision_tick/max_squads);RL 训练超参(0.05/0.005/10/近邻5)显式命名为训练config(非游戏规则)。done-when:rust 后端 100 步不崩+确定性、selfplay rust 短训 return 有界不发散〕
- [x] Web：HexRenderer 渲染真态势 + ReplayViewer 真重放〔T5.3；HexRenderer 加 drawUnits(按方着色标记+id/班车数)/drawGridFor/clear;ReplayViewer 重写:挂载 PIXI 应用+HexRenderer,载入 replay JSON,按时间轴 t 取最近周期 snapshot 渲染真态势(units/control),被载单位不单独显(规则#5);llm_agent 每 tick 写周期 snapshots 供真重放;修构建:tsconfig ES2022+noEmit、去 React 死导入、inline-css 移出 index.html→styles.css。**done-when npm run build 已通过(本地 no-space 路径验证 ✓built),Z:网络盘 UNC"Shared Folders"空格致 vite/rollup 路径错乱是环境限制非代码(vite.config 注明);ReplayViewer 载入并渲染 replay**〕
- [x] 编辑器·地图：六角刷高程/地物/设施 + 导入(JSON/TMX)/校验/导出（`web/src/editor/MapEditor.tsx`）〔T5.6；可点击 SVG 六角网格(按地物着色+高程标注),点击落笔:高程/地物两笔刷(设施属想定层非地图 hex 字段,故归想定编辑器);载入内置 demo_valley→编辑→导出经 resourceIO.validate→ajv map.schema;修地物枚举对齐 schema(open 非 plain)。done-when:npm run build 通过(本地无空格路径 ✓built),编辑后地图过 map.schema 校验(0 错)〕
- [x] 编辑器·想定：布阵/夺控/胜负 + 导入/校验/导出（`ScenarioEditor.tsx`）〔T5.7；在所选地图(载内置 demo_valley 网格)上点击布阵:选阵营/算子类型/班车数→点格放置(生成唯一 id、push 到 sides[side].units、覆盖标记),既有算子按方着色叠加;导入/导出经 resourceIO.validate→ajv scenario.schema。done-when:npm run build 通过(本地 ✓built),布阵后想定过 scenario.schema 校验(0 错)〕
- [x] 编辑器·规则：规则总表 + 18 张裁决表表格化编辑 + 导入/校验/导出（`RulesEditor.tsx`）〔T5.x;规则总表 tab 把 timing/control/fortification/minefield/air 各标量叶渲染成可编辑数字表(就地改值),导入/导出整套规则经 resourceIO.validate→ajv rules.schema;裁决表 tab 选 18 张表逐张导入/导出(未核对表显待核验警告),权威真值仍以双人交叉核验为准。done-when:npm run build 通过(本地 ✓built),编辑标量后规则过 rules.schema(0 错)〕
- [x] 资源模型：内置只读基线 + 用户资源导入/导出闭环（`resourceIO.ts` / `docs/RESOURCES.md`）；引擎可加载用户资源〔resourceIO(内置基线 loadBuiltin + 文件导入 pickFile/parseTextToData + ajv 校验 + 导出 downloadJSON)已全;RESOURCES.md 文档齐;三编辑器(T5.6/T5.7/规则)均经此闭环产出校验过的资源。新增 tools/resource_smoke.py:任一资源(内置或编辑器导出的 user 资源)先过 schema 再灌入 Rust 引擎跑 N tick——已验证内置 demo + 用户改造想定(放 UGV 隶属 R-T1)均 schema-valid 且引擎加载运行(红方见 3 单位含新放 RE0),证明"编辑器一套/引擎一套"无漂移〕
- [x] 概率 provider：StaticTable→Bayesian 校准闭环（`/calibrate-prob`）〔T5.4；实现 prob::BayesianTable::resolve(原 todo!()):按 attack_level 取校准行(best_row 同 static 截断)→从后验权重 params.byLevel[level][outcome]=α 确定性采类别(key 排序+next_u32_below/DENOM 均匀抽样,硬规则#1),无 panic(硬规则#4);calibrate.py 修 UTF-8 编码,跑出 config/rules.calibrated.json(direct_vs_personnel static→bayesian,余表不变);schema 加 bayesian params.byLevel 条件约束(规则#3);validate 纳入 calibrated。done-when:某表切 bayesian 后 cargo test 56 绿(含 prob::bayesian 确定性+仅采样行内结果),make validate 绿〕
- [x] 美术：核心算子/地形贴图经 `/gen-art` 出齐并入 manifest 〔T5.5;**50 张全出齐**:地形 10(旷野/居民地/丛林/小河/大河/湖泊/松软地/公路/铁路+高程带,RGB)+算子 16×红蓝两色=32(坦克/IFV/步兵/炮兵/无人机/武直/运直/巡飞弹/侦察战车/防空高炮/导弹小队/导弹车/无人战车/布雷车/扫雷车/天基侦察)+设施 5+UI 3,后 40 张走 rembg/U²-Net 抠底为透明 RGBA;manifest 50 项与盘面一一对应、make validate 绿。走 Codex 订阅出图(auth_mode chatgpt,不计费)。修了 gen_art 三处真 bug:UTF-8 编码、捞图(Codex 存到 ~/.codex/generated_images 自取)、**从本地 cwd 跑 codex**(Z: 网络盘 UNC cwd 致其 imagegen 沙箱 CreateProcessWithLogonW 失败退化成 SVG);并续跑/抠底/manifest 健壮化。assets/generated/ 为 gitignored 本地产物,可由已提交的 pack+gen_art 复现〕

## 数值复核欠账（落地前必须做）
- [x] 复核 `attack_level_vs_vehicle.json`：中号炮 d14/15、小号炮 d11-13〔2 读者共识，大号精确吻合验证对齐〕
- [x] **录入** `result_vs_vehicle.json` 段1+段2 全格值 —— 全局最关键〔3 读者共识转写，T1.7 前置〕
- [x] 复核 `height_diff_correction.json` 全矩阵〔3 读者共识，修正了中段/尾列多处〕
- [x] 复核 `result_vs_personnel.json` 随机数 6-10 段〔2 读者逐格一致，修正了中段〕
- [x] 复核 `aa_attack_level.json`〔逐格复核(3.4x 列对齐放大)：修正 便携防空导弹中段(7 7 7 6 6)与车载防空导弹(八个7,d3..d10);该表是横向滚动控件,仅渲染 d0..d19,车载 d20..d50 在页外取可见平台值6〕
